#' Field Description: Information Rates
#' @field informationRates The information rates (that must be fixed prior to the trial), 
#'   default is \code{(1:kMax) / kMax}. Is a numeric vector of length \code{kMax} (the maximum number of stages).
#' @keywords internal
