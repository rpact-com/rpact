#' Field description: reversal probabilities
#' @field reversalProbabilities The probability to switch from stopping the trial for success (or futility) and reaching non-rejection (or rejection) in a delayed response design. Is a numeric vector of length \code{kMax} minus 1 containing values between 0 and 1.
#' @keywords internal
