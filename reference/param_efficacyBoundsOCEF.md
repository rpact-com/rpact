# Parameter Description: Efficacy boundary (optimal conditional error design)

Parameter Description: Efficacy boundary (optimal conditional error
design)

## Arguments

- efficacyBounds:

  Stage 1 efficacy boundary \\\alpha_1\\ (p-value scale). Must be a
  numeric value between 0 and 1. Should be smaller than
  `futilityBounds`.
